#include <bits/stdc++.h>

using namespace::std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(NULL);

    int n; cin >> n;

    for (int i = 1; i <= n; i++) {
        cout << i << " Abracadabra\n";
    }

    return 0;
}
